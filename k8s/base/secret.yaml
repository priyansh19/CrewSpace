apiVersion: v1
kind: Secret
metadata:
  name: crewspace-secrets
  namespace: crewspace
type: Opaque
stringData:
  # Generate with: openssl rand -hex 32
  BETTER_AUTH_SECRET: "REPLACE_ME"
  CREWSPACE_AGENT_JWT_SECRET: "REPLACE_ME"
  DATABASE_URL: "postgres://crewspace:REPLACE_DB_PASSWORD@crewspace-db:5432/crewspace"
